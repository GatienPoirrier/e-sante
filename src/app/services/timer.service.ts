import { Injectable } from "@angular/core";
import { Observable } from "rxjs";
import { AirSensor } from "../sensor";

@Injectable({
    providedIn: 'root'
  })
  export class TimerService {
      
//   timer = new Observable().timer(3000) ;
  }